# Cargo Build system for Sublimetext
Based on the default cargo build system bundled in the Rust package from Sublime text (https://github.com/sublimehq/Packages/tree/master/Rust).

* Added update (--dry-run) and format:
* Change commands to ensure no color output is used (except run)
* Change the syntax highlighting for the added commands and parsing of URLs in clippy to open in browser
